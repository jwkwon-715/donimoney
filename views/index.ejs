<!DOCTYPE html>
<html>
<head>
  <title>메인 페이지</title>
</head>
<body>
  <h1>메인 화면</h1>

  <% if (isAuthenticated) { %>
    <p>👋 <strong><%= user.user_id %></strong>님, 환영합니다!</p>
    <a href="/users/logout">로그아웃</a>
  <% } else { %>
    <p>🔒 로그인하지 않은 사용자입니다.</p>
    <a href="/users/login">로그인</a>
    <a href="/users/signup">회원가입</a>
  <% } %>

  <!-- 로그인 여부 상관없이 시작하기 버튼은 항상 보여줌 -->
  <form action="/game/start" method="GET">
    <button type="submit">시작하기</button>
  </form>

</body>
</html>
